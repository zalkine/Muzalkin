// TODO: Renders a song's chords + lyrics line by line.
// chords_data shape: [{ type: "chords" | "lyrics" | "section", content: string }]
// Respects RTL direction from hooks/useLanguage.ts.
export {};
